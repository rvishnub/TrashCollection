@model IEnumerable<TrashCollection.Models.CustomerPickupJuncture>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Customer.FirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Pickup.PickupID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Customer.FirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Pickup.PickupID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CustomerPickupJunctureID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CustomerPickupJunctureID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CustomerPickupJunctureID })
        </td>
    </tr>
}

</table>
